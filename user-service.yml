---
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/user-mgmt
    username: root
    password: root
    driver-class-name: com.mysql.jdbc.Driver
  jpa:
    database-platform: org.hibernate.dialect.MySQL5Dialect
    show-sql: true
    hibernate:
      ddl-auto: create
  application:
    name: student-service
server:
  port: 3000 
